
export const config = {
    todosUrl: 'http://localhost:3001/api/v1/tasks'
    // loginUrl: `http://${window.location.hostname}/api/login`,
    // todosUrl: `http://${window.location.hostname}/api/todos`
}